{
  "name": "vfd-platform",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev:web": "pnpm --filter web dev",
    "dev:api": "cd apps/api && uv run uvicorn main:app --reload --host 0.0.0.0 --port 8000",
    "dev": "concurrently -n web,api -c blue,green \"pnpm dev:web\" \"pnpm dev:api\"",
    "build:web": "pnpm --filter web build",
    "build": "pnpm build:web",
    "start": "pnpm --filter web start",
    "db:up": "docker compose up -d postgres redis",
    "db:down": "docker compose down",
    "db:migrate": "cd apps/api && uv run alembic upgrade head",
    "db:revision": "cd apps/api && uv run alembic revision --autogenerate -m"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "packageManager": "pnpm@10.26.1"
}
